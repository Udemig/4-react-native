let character = 'sonic';
let km = 90000;
let showModal = false;

// character = 35;
character = 'mario';

// km = "az";

// showModal = "açık";

// fonksiyon'ların parametresine tip ataması yapma
const katla = (deger: number) => {
  return deger * 5;
};

console.log(katla(600));
// katla('selam');
// katla([]);
// katla('selam');
// katla({});
